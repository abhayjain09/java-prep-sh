# angular/

**Status:** Not started — coming in Module 9, as the frontend for the Order/Inventory REST API built in `spring/`.

Planned coverage:
- Angular fundamentals → advanced: components, standalone components, routing, lazy loading.
- State/reactivity: RxJS, Signals, forms (template-driven and reactive).
- DI, HTTP interceptors (attaching JWTs from `security/`), route guards.
- Change detection strategies and performance tuning.
- A small UI over the Order/Inventory API: product catalog, order creation, order status tracking.

See the root [README.md](../README.md) for the full module roadmap and current progress.
