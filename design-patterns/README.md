# design-patterns/

**Status:** Not started — coming in Module 4.

Planned coverage:
- SOLID and GRASP principles, each shown as a violation-in-the-domain first, then corrected.
- All GoF design patterns (creational, structural, behavioral), each with a wrong (naive) implementation in the Order/Inventory domain and a corrected one — e.g. a naive `if/else` chain for payment types refactored into Strategy, a God-object `OrderManager` refactored using Facade/Command.

See the root [README.md](../README.md) for the full module roadmap and current progress.
