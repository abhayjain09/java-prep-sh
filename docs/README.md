# docs/

Cross-cutting documentation that doesn't belong to a single module: architecture decision records (ADRs), the overall domain glossary, and diagrams that span multiple modules (e.g. the final target architecture once Spring + Angular + AWS modules exist).

**Status:** Ongoing — will accumulate as later modules are built. Nothing here yet beyond this stub.

See the root [README.md](../README.md) for the full module roadmap.
