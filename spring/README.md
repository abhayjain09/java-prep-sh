# spring/

**Status:** Not started — coming in Module 5 (Spring Core, Spring Boot, Spring Data JPA, REST APIs), with caching (Module 8) added later.

Planned coverage:
- Spring Core: IoC container, bean lifecycle, dependency injection styles, profiles, configuration.
- Spring Boot: auto-configuration, starters, actuator, externalized config.
- Spring Data JPA: repositories, entity mapping for `Customer`/`Product`/`Order`/`OrderLine`/`Inventory`, relationships, transactions.
- REST APIs: CRUD endpoints over the Order/Inventory domain, request validation, versioning, pagination, sorting, filtering, HATEOAS overview, OpenAPI docs, global exception handling.
- Caching: Spring Cache abstraction, Redis-backed caching patterns, TTL, eviction strategies for inventory lookups.

See the root [README.md](../README.md) for the full module roadmap and current progress.
